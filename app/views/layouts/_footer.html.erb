<footer class="bg-white border-t border-gray-200 mt-12">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 items-start">
      <div>
        <h2 class="text-2xl font-bold text-purple-700 tracking-tight">
          Rails <span class="text-gray-900">Refresh</span>
        </h2>
        <p class="mt-2 text-sm text-gray-500 max-w-xs">
          Just something to refresh some rails knowledge.
        </p>
      </div>

      <div class="md:col-span-1 flex flex-col space-y-2 text-sm font-medium text-gray-600">
        <%= link_to "Videos", posts_path, class: "hover:text-purple-600 transition" %>
        <%= link_to "Pricing", pricing_path, class: "hover:text-purple-600 transition" %>
        <%#= link_to "About", about_path, class: "hover:text-purple-600 transition" %>
      </div>
    </div>

    <div class="mt-10 border-t pt-6 text-sm text-gray-400 text-center">
      &copy; <%= Time.now.year %> Rails Refresh By Matt Lutrick.
    </div>
  </div>
</footer>